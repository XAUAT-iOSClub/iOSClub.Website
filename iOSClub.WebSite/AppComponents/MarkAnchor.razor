@using iOSClub.WebSite.Models

<AnchorLink Href="@Model.Link" Title="@Model.Name">
    @foreach (var s in Model.Children)
    {
        <MarkAnchor Model="@s"/>
    }
</AnchorLink>

@code {
    [Parameter] public MarkAnchorModel Model { get; set; } = new();
}